import { Map } from "../../Map/Map";

export class WMSOptions {
  constructor(
    url: String,
    blend: Boolean,
    zooms: [Number, Number],
    tileSize: Number,
    opacity: Number,
    zIndex: Number,
    visible: Boolean,
    params: Object
  )

}