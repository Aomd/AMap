import { Bounds } from "../Bounds";

export class CitySearchResult {

  /**
   *Creates an instance of CitySearchResult.
   * @param {String} city 城市名称
   * @param {Bounds} bounds 地图展示该城市时使用的矩形区域
   * @memberof CitySearchResult
   */
  constructor(
    city: String,
    bounds: Bounds
  )
}