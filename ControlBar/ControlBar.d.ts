import { ControlBarOptions } from './ControlBarOptions';


/**
 * 组合了旋转、倾斜、复位、缩放在内的地图控件，自V1.4.0版本新增。
 *
 * @export
 * @class ControlBar
 */
export class ControlBar {

  /**
   *Creates an instance of ControlBar.
   * @param {(Object|ControlBarOptions)} opt
   * @memberof ControlBar
   */
  constructor(
    opt: Object | ControlBarOptions
  )
}